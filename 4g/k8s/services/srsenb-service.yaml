apiVersion: v1
kind: Service
metadata:
  name: srsenb
  labels:
    app: srsenb
spec:
  selector:
    app: srsenb
  ports:
    - name: zmq-tx
      protocol: TCP
      port: 2000
      targetPort: 2000
  clusterIP: None
